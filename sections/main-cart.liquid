{% form 'cart', cart %}
  
<h2>cart</h2>

{% for item in cart.items %}
        <div>
            <img src="{{ item.image | image_url: width: 200 }}"/>
            <h2>{{ item.title }}</h2>
            <p>{{ item.price | money }}</p>
        </div>
{% endfor %}

<p>{{ cart.empty? }}</p>

{% endform %}